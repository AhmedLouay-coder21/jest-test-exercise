# Jest Test Exercise

This repository was created as a practice project for testing JavaScript functions using Jest. It serves as a practical implementation of unit testing with Jest.

Babel is also used to enable the use of `import` and `export` syntax instead of `require`.
